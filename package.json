{
  "name": "hidromedusa",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx serve public -p 3000",
    "db:up": "docker compose -f deploy/localstack/docker-compose.yml up -d",
    "db:down": "docker compose -f deploy/localstack/docker-compose.yml down",
    "db:init": "bash deploy/localstack/create-tables.sh"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
